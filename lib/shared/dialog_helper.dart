import 'package:flutter/material.dart';

class DialogHelper {
  // show dialog
  static void showSuccessDialog(
      {required String title, required String subtitle, String? imagePath, Widget? child, bool isDismissible = true}) {
    // same with show dialog in flutter
    // CustomDialog(
    //   title: description,
    //   subtitle: subtitle.isNotEmpty ? subtitle : 'Search product currently unavailable, please try again later',
    // )
  }

  // show dialog
  static void showErrorDialog({required String description, String subtitle = ''}) {
    // same with show dialog in flutter

    // CustomDialog(
    //   title: description,
    //   subtitle: subtitle.isNotEmpty ? subtitle : 'Search product currently unavailable, please try again later',
    // )
  }
  // show toast
  // show snackbar
  // show loading dialog
  // show hide dialog

}
